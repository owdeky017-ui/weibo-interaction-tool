# 产物溯源（PROVENANCE）

这个项目从零开发、一路做到打包分发，中间经历过一次「源码散失、只剩打包产物」，
再据产物还原出源码继续开发。所以「哪份东西是权威的、哪份没被动过」必须写清楚 ——
否则很容易改错目录。

## 溯源链

```
① 早期打包的分发版（只读）
        │  还原：展开 PYZ → 读字节码 → 重建模块结构与函数签名
        ▼
② 据此还原出的源码基线（已冻结）
        │  工程化：性能 / 健壮性 / 加密 / 三层验证
        ▼
③ 本仓库（唯一继续开发的目录）
        │  打包：build.py 写 A_MODE → PyInstaller → 组装 → 三层自检
        ▼
④ 可分发的构建产物（扫码A版）
```

## 目录对照

| 阶段 | 路径 | 版本控制 | 说明 |
|---|---|---|---|
| ① | `<早期分发包目录>` | 无（不需要） | 早期分发版，仅作结构对照。**全程只读，未改动** |
| ② | `<基线源码目录>` | git（单提交基线） | 据产物还原出的源码，工程化**前**的状态 |
| ③ | `<本仓库>` | git（本仓库） | **唯一权威源**，所有开发在这里进行 |
| ④ | `<构建产物目录>` | 无（由 `build.py` 生成） | 可分发构建产物，用户A = 当前扫码登录账号 |

## 校验值

```
# ① 早期分发版 —— 前后必须保持一致
<早期分发包目录>\WeiboInteractionQuery.exe
  MD5   2b13a21e449ee853ad859b19568472fd
  大小  11,008,221 字节

# ④ 构建产物（目录内 exe 与分发 zip 内的 exe 哈希一致）
<构建产物目录>\WeiboInteractionQuery.exe
  MD5   12030d3ef3a98b38c75db69b477b4221
  大小   5,617,114 字节
```

原始分发包体积 11.0 MB，构建产物 5.6 MB —— 差掉的约 5.4 MB 主要来自
移除 pandas / numpy，以及 PyInstaller 版本与选项的差异。

## 敏感数据位置

以下路径在程序运行后会存放微博登录态，**不要复制、不要提交、不要打包分发**：

- `<基线源码目录>\data\cookies.json` —— 工程化前，明文格式
- `<本仓库>\data\` —— 工程化后，DPAPI 加密格式（源码直跑时生成）
- `<构建产物目录>\data\` —— DPAPI 加密格式（打包版运行后生成）

两个仓库的 `.gitignore` 都已排除 `data/`。
`build.py` 的自检里另有一条断言：分发包的 `data/` 不得携带任何运行时数据。

## 重建方式

```powershell
cd <本仓库>
python build.py            # 打包 + 组装 + 三层自检
```

产物会重新写到 `<构建产物目录>`。
